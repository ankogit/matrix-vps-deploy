# LiveKit + Element lk-jwt-service.
# Опционально coturn: docker-compose.coturn.yml (см. README).
# Continuwuity, Nginx, Certbot — на хосте.

services:
  livekit:
    image: livekit/livekit-server:v1.9.4
    container_name: livekit
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config/livekit.yaml:/etc/livekit.yaml:ro
    command: ["--config", "/etc/livekit.yaml"]

  lk-jwt:
    image: ghcr.io/element-hq/lk-jwt-service:0.4.2
    container_name: lk-jwt
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      LIVEKIT_URL: ${LIVEKIT_URL}
      LIVEKIT_KEY: ${LIVEKIT_API_KEY}
      LIVEKIT_SECRET: ${LIVEKIT_API_SECRET}
      LIVEKIT_FULL_ACCESS_HOMESERVERS: ${LIVEKIT_FULL_ACCESS_HOMESERVERS}
    # host network у livekit — зависимость только для порядка запуска
    depends_on:
      - livekit
